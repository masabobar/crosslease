pnpm-lock.yaml

# Generated by `pnpm fetch:openapi`, never hand-edited — eslint.config.js ignores it too.
# Formatting it is not just churn: the pre-push drift check compares this file against a
# freshly generated one, so any formatting the generator does not itself produce reads as
# backend drift and fails every push.
src/generated/
